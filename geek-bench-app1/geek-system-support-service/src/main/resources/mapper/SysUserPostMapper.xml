<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fosung.system.support.system.dao.sys.SysUserPostDao">

    <delete id="deleteByPostId" >
        delete from sys_user_role where app_id = #{appId} and role_id = #{roleId}
    </delete>

    <delete id="deleteByUserId" >
        delete from sys_user_role where app_id = #{appId} and user_id = #{userId}
    </delete>

</mapper>