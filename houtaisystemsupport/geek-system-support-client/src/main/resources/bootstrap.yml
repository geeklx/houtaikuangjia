spring:
  profiles:
    active: ${SPRING_CONFIG_PROFILE:dev}
  application:
    name: ${SPRING_APPLICATION_NAME:geek-system}
  cloud:
    config:
      label: ${SPRING_CONFIG_LABEL:master}
      uri: ${SPRING_CONFIG_URI:}